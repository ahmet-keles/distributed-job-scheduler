# Copy to .env and fill in local values: cp .env.example .env
# Shared by Docker Compose and by the app when run on the host.

POSTGRES_DB=jobs
POSTGRES_USER=jobs_user
POSTGRES_PASSWORD=change_me
POSTGRES_HOST=127.0.0.1
POSTGRES_PORT=5440
